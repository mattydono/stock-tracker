import React from 'react';

const CompanyOverview = ({ tags, companyName, symbol, website, description }) => {

    console.log(tags, companyName, symbol, website, description);

    return null;
}

export default CompanyOverview;